apiVersion: v1
kind: Pod
metadate:
  name: voting-app-pod
  labels:
    name: voting-app-pod
    app: demo-voting-app
spec:
  containers:
  - name: voting-app  
    image: kodecloud/examplevotingapp_vote:v1
    ports:
      - containerPort: 80